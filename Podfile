platform :ios, '12.0'

target 'MobileEra' do
  use_frameworks!

  pod 'SDWebImage'
  pod 'R.swift'

  pod 'Firebase/Core'
  pod 'Firebase/Firestore'
  pod 'FirebaseFirestoreSwift'
  # Needed because of a crash with Firebase/Core
  pod 'nanopb', '0.3.901'
end
